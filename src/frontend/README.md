# Frontend

SPA React + TypeScript creada con Vite y organizada por funcionalidad.

```powershell
npm install
npm run dev
```

La pantalla inicial es una base visual sin conexión a base de datos ni a
dispositivos. La primera vertical conectará `features/line-identification` con
la API.

