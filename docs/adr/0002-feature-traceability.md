# ADR 0002: trazabilidad por nombre funcional

Estado: aceptada.

Cada funcionalidad se localiza mediante una entrada en `functional-map.md`. Su
documentación, caso de uso backend, feature frontend y pruebas comparten un
nombre reconocible. Esto reduce exploraciones globales y mantiene los cambios
acotados.

