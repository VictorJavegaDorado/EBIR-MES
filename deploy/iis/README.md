# IIS

Pendiente de definir bindings, identidad del application pool, permisos de
carpeta, variables de entorno y procedimiento de rollback en el servidor MES.
No se almacenarán secretos en `web.config`.

